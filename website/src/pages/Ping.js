import React from 'react';
import { Link } from 'react-router';

const Ping = ({}) => (
    <div>
        <h1>Ping Page</h1>
        <Link to='/login'>Go to login</Link>
    </div>
)

export default Ping;